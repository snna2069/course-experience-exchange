// LEGACY / INACTIVE: only consumed by the legacy Kafka producer/consumer
// scripts in this folder, which are not wired into the active backend.
module.exports = {
    kafkaHost: "localhost:9092",
    topics: {
      courseEvents: "courseEvents",
    },
  };
  